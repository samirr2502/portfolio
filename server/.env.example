# Copy this file to server/.env and paste your OpenAI key.
# server/.env is gitignored — never commit real keys.

# Required — get one at https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your-openai-key-here

# Optional
OPENAI_MODEL=gpt-4o-mini
PORT=3020
MESSAGE_LIMIT=25
